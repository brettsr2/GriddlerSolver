<Window x:Class="Griddler_Solver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Griddler_Solver"
        mc:Ignorable="d"
        Title="Nonogram solver" Width="1024" Height="780" Icon="/app.ico" Closing="OnWindow_Closing">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>

    <!-- URL bar -->
    <Grid Grid.Row="0" Grid.ColumnSpan="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <Label Grid.Column="0" Content="Url:" Margin="4"/>
      <ComboBox x:Name="comboBoxUrl" Grid.Column="1" IsEditable="True" SelectedIndex="0" MinHeight="26" VerticalAlignment="Center" VerticalContentAlignment="Center" SelectionChanged="OnComboBoxUrl_SelectionChanged"/>
      <Button Grid.Column="2" Margin="5 4 4 4" MinWidth="100" Content="Download" Click="OnButtonDownload_Click"/>
      <Button Grid.Column="3" Margin="10 4 4 4" MinWidth="100" Content="Save" Click="OnButtonSave_Click"/>
      <Button Grid.Column="4" Margin="5 4 5 4" MinWidth="100" Content="Load" Click="OnButtonLoad_Click"/>
    </Grid>

    <!-- Info label -->
    <Label x:Name="label" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" Padding="4 2" IsHitTestVisible="False"/>

    <!-- Board canvas -->
    <Grid Grid.Row="2" Grid.Column="0">
      <local:BoardCanvas x:Name="boardCanvas" ClipToBounds="True" SizeChanged="OnCanvas_SizeChanged" MouseMove="OnCanvas_MouseMove"/>
    </Grid>

    <!-- Options panel -->
    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Vertical" Margin="4 4 8 4">
      <CheckBox x:Name="checkBoxLineSolving" Margin="4 6 4 0" VerticalAlignment="Center" IsChecked="True" IsEnabled="False">Line Solving</CheckBox>
      <CheckBox x:Name="checkBoxMultithread" Margin="4 6 4 0" VerticalAlignment="Center" IsChecked="{Binding CheckBoxMultithread}">Multithreads</CheckBox>
      <Separator Margin="4 8 4 0"/>
      <Button x:Name="buttonClearBoard" Margin="4 6 4 0" MinHeight="26" Click="OnButtonClearBoard_Click" Content="Clear board"/>
      <Button x:Name="buttonSolve" Margin="4 8 4 0" MinHeight="26" Click="OnButtonSolve_Click" Content="Solve"/>
      <Separator Margin="4 8 4 0"/>
      <Label x:Name="labelCoordinates" Margin="0 8 0 0" MinWidth="130" Padding="4 0" VerticalAlignment="Center"/>
    </StackPanel>
  </Grid>
</Window>
