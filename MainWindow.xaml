<Window x:Class="Griddler_Solver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Griddler_Solver"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="Url:" />
        <TextBlock Grid.Column="1" />
        <Button Grid.Column="2" Content="Go" Click="OnButtonGo_Click"></Button>
      </Grid>
    </StackPanel>
    <Canvas x:Name="canvas" Grid.Row="1" MouseDown="OnCanvas_MouseDown"/>
    <Button x:Name="loadInput" Grid.Row="2" Content="Load input image" Click="OnLoadInput_Click"/>
    <Button x:Name="solve" Grid.Row="3" Content="Solve" Click="OnSolve_Click"/>
  </Grid>
</Window>
